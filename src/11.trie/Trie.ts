export class Trie<V> {
  size = 0;
  startsWith(val: {val: string}): boolean {
    throw new Error("");
  }
  add(k: string, v: V): V {
    throw new Error("");
  }
  get(val: string): V {
    throw new Error("");
  }
  remove(val: string): V {
    throw new Error("");
  }
}
